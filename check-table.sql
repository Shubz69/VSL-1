-- First, make sure you're using the railway database
USE railway;

-- Check if the table exists and show its structure
SHOW TABLES LIKE 'applications';

-- Show the table structure if it exists
DESCRIBE applications;

-- View all data in the table (will be empty if no submissions yet)
SELECT * FROM applications;


